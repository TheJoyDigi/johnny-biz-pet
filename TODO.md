- [x] **Enable PostGIS:** Verify PostGIS extension is enabled in Supabase to support efficient geospatial queries.
- [x] **Add Geography Column:** Create a migration to add a `location` column (type `GEOGRAPHY(POINT, 4326)`) to the `sitters` table.
  - [x] Create a trigger or update logic to automatically populate `location` from `lat`/`lng` columns to ensure consistency.
- [x] **Create Search Function:** Implement a Supabase RPC function (e.g., `search_sitters_nearby`) to allow querying sitters within a radius, sorted by distance.
- [x] **Update Sitter API:** Update `src/pages/api/admin/update-sitter.ts` to ensure the `location` column is updated when the admin saves a profile.
- [x] **Frontend Search:**
  - [x] Implement a "Find a Sitter" search input on the public Sitters page.
  - [x] Integrate with Google Places Autocomplete (or similar) to get user coordinates.
  - [x] Connect the search input to the `search_sitters_nearby` RPC to filter results.
- [x] look into best option for admin sitter edit adress and database to store sitter address geolocation